name: PR sync handler
run-name: Pull request ${{github.event.action}} for PR ${{ github.event.number}} in ${{ github.repository }}
on:
  pull_request:
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎  Your repository is ${{ github.repository }}."
      - run: echo "🔎  Your event action type is is ${{ github.event.action }}."
      - run: echo "🔎  This pull request has a mergeable state of ${{ github.event.pull_request.mergeable }}."
      - run: echo "🔎  This pull request or ${{ github.pull_request.mergeable }}."
      - run: echo "🔎  Your base ref is ${{ github.base_ref }}."
      - run: echo "🔎  Your github ref is ${{ github.ref }}."
      - run: echo "🔎  Your github SHA is ${{ github.sha }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
